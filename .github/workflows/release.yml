name: Release

# Publishes when the version changes, and only then.
#
# The version lives in one place: `[workspace.package] version` in the root
# Cargo.toml. `apps/gui/tauri.conf.json` deliberately omits its `version` field
# so Tauri falls back to that, which means one bump moves the bundle version,
# the in-app build stamp (`az_core::VERSION`) and the number the updater
# compares, together. Tauri's docs suggest keeping the version in the config
# instead; that advice does not fit a repo where the build stamp already reads
# the crate version, and two declarations are two things to forget.
#
# "Changed" is decided against what is actually published, not against git
# history. Comparing to the live latest.json makes this idempotent: a release
# that failed halfway is retried by the next push, a re-run is harmless, and
# squashes and force-pushes cannot confuse it.
#
# Publishing overwrites a fixed, versionless CDN path, so latest.json is the
# only record of the current version. That is why the purges below are hard
# failures: a stale edge copy is indistinguishable, to a client, from a release
# that never happened.
#
# The publish choreography follows the 24x.ai site pipeline, the one BunnyCDN
# flow here that has never served stale or corrupt bytes: DELETE the storage
# object before uploading its replacement, and purge the whole pull zone rather
# than one URL. Overwriting in place is how the 0.1.1 release got poisoned — an
# edge pulled mid-overwrite, cached a 206 partial response as the full object,
# and then served a truncated gzip for as long as its ~300-day max-age allowed.
# The per-URL purge endpoint made it worse by reporting success without
# evicting that object; three purge attempts changed nothing. A zone-wide purge
# does evict, and it is not the collateral damage it sounds like: the site
# fronted by this same zone purges the zone on every one of its own deploys.

on:
  push:
    branches:
      - master
  workflow_dispatch:
    inputs:
      force:
        description: "Publish even if the version has not changed"
        type: boolean
        default: false

concurrency:
  group: release
  cancel-in-progress: false

env:
  CDN_DIR: agencyzero
  CDN_BASE: https://24x.ai/agencyzero
  BUNDLE_DIR: target/release/bundle/macos

jobs:
  # Cheap gate, so a macOS runner is only spent on pushes that actually ship.
  # This half has no macOS dependency, so it follows the usual runner rule.
  check:
    runs-on: ubicloud-standard-2
    outputs:
      version: ${{ steps.decide.outputs.version }}
      publish: ${{ steps.decide.outputs.publish }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: One rev per git source
        # Cheaper to fail on this runner than twenty minutes into a macOS
        # bundle, which is the only build that resolves the renderer at all.
        run: scripts/check-one-rev-per-git-source.sh

      - name: Compare the committed version against the published one
        id: decide
        run: |
          VERSION=$(python3 -c "import tomllib;print(tomllib.load(open('Cargo.toml','rb'))['workspace']['package']['version'])")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

          LIVE=$(curl -fsS --max-time 30 "$CDN_BASE/latest.json" 2>/dev/null \
            | python3 -c 'import json,sys; print(json.load(sys.stdin)["version"])' 2>/dev/null || echo "")

          if [ "${{ inputs.force }}" = "true" ]; then
            echo "publish=true" >> "$GITHUB_OUTPUT"
            echo "Forced: publishing $VERSION over live '${LIVE:-none}'."
          elif [ -z "$LIVE" ]; then
            echo "publish=true" >> "$GITHUB_OUTPUT"
            echo "Nothing published yet, so $VERSION is the first release."
          elif [ "$LIVE" != "$VERSION" ]; then
            echo "publish=true" >> "$GITHUB_OUTPUT"
            echo "Committed $VERSION differs from live $LIVE, publishing."
          else
            echo "publish=false" >> "$GITHUB_OUTPUT"
            echo "Live is already $LIVE; nothing to do. Bump [workspace.package] version to release."
          fi

  release:
    needs: check
    if: needs.check.outputs.publish == 'true'
    # The only job in this repository that cannot use ubicloud-standard-*:
    # Ubicloud offers Linux runners only, and a Tauri macOS bundle needs the
    # macOS SDK, WebKit and Apple's signing tools. This runner is arm64, so
    # the build is native rather than cross.
    #
    # Namespace, verified macOS on 2026-08-01: a trial of this profile on the
    # frontend job reported `Operating System: macOS` and pulled
    # bun-darwin-aarch64. Two runners in total now, this and ubicloud.
    runs-on: namespace-profile-agency-tahoe

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Cache cargo build
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: agencyzero-macos-release
          cache-targets: true

      - name: Install frontend dependencies
        run: bun install --frozen-lockfile
        working-directory: apps/gui/frontend

      - name: Build the AgencyProxy sidecar
        run: scripts/stage-agency-proxy-sidecar.sh

      # Ad-hoc signed, and nothing here decides that: `signingIdentity: "-"` in
      # tauri.conf.json does. Notarization needs a current Apple Developer
      # Program membership and ours has lapsed.
      #
      # Do not reintroduce `APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}`
      # as a way to detect whether a certificate exists. A secret that does not
      # exist still defines the variable, as an empty string, and Tauri reads
      # "defined" as "import a certificate", then dies with `SecKeychainItem
      # Import: One or more parameters passed to a function were not valid`
      # after a full release build. Absent and empty are not the same thing
      # here, so the Apple variables are simply not passed.
      #
      # On renewal: create a Developer ID Application certificate, drop
      # `signingIdentity` from tauri.conf.json, and add APPLE_CERTIFICATE,
      # APPLE_CERTIFICATE_PASSWORD, APPLE_API_ISSUER, APPLE_API_KEY and
      # APPLE_API_KEY_PATH below unconditionally. Tauri notarizes and staples
      # on its own once they are present.
      - name: Build bundle
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""
          # See the same block in release-experimental.yml. A crash report or a
          # `sample` from a stripped binary names no function of ours, and both
          # of this week's defects were found only once one did.
          CARGO_PROFILE_RELEASE_STRIP: "false"
          CARGO_PROFILE_RELEASE_DEBUG: line-tables-only
        run: bunx --yes @tauri-apps/cli@^2.11 build --features blitz-inspector
        working-directory: apps/gui

      # Two failures worth catching here. The signature one was live in this
      # repo: a bundle whose inner executable is linker-ad-hoc-signed while the
      # bundle has no sealed resources verifies nowhere, and the symptom on a
      # user's Mac is "damaged and can't be opened" rather than anything naming
      # signing. The version one guards the indirection above: if the Cargo.toml
      # fallback ever stops feeding the bundle, latest.json would advertise a
      # version the app does not report, and every client would re-download the
      # same build forever.
      - name: Verify the bundle
        env:
          VERSION: ${{ needs.check.outputs.version }}
        run: |
          APP="$BUNDLE_DIR/AgencyZero.app"
          codesign --verify --deep --strict --verbose=2 "$APP"
          codesign -dv --verbose=4 "$APP" 2>&1 | grep -E "Signature|TeamIdentifier|Sealed Resources"

          BUILT=$(/usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" "$APP/Contents/Info.plist")
          if [ "$BUILT" != "$VERSION" ]; then
            echo "::error::bundle reports $BUILT but this release is $VERSION"
            exit 1
          fi
          echo "bundle version $BUILT matches"

          # Assert the bundle really is ad-hoc. If a certificate ever gets wired
          # in, this fires and is the reminder to drop the cask's quarantine
          # postflight and add `spctl -a` plus `stapler validate` here instead.
          if codesign -dv --verbose=4 "$APP" 2>&1 | grep -q "TeamIdentifier=not set"; then
            echo "ad-hoc as expected"
          else
            echo "::error::bundle is no longer ad-hoc; update the verification and the cask"
            exit 1
          fi

      - name: Build latest.json
        env:
          VERSION: ${{ needs.check.outputs.version }}
        run: |
          TARBALL="$BUNDLE_DIR/AgencyZero.app.tar.gz"
          test -f "$TARBALL" || { echo "::error::missing $TARBALL"; exit 1; }
          test -f "$TARBALL.sig" || { echo "::error::missing $TARBALL.sig (was TAURI_SIGNING_PRIVATE_KEY set?)"; exit 1; }
          python3 - "$VERSION" "$TARBALL.sig" "$CDN_BASE" <<'PY'
          import json, sys, datetime
          version, sig_path, base = sys.argv[1], sys.argv[2], sys.argv[3]
          with open(sig_path) as f:
              signature = f.read().strip()
          manifest = {
              "version": version,
              "pub_date": datetime.datetime.now(datetime.timezone.utc)
                  .strftime("%Y-%m-%dT%H:%M:%SZ"),
              "platforms": {
                  "darwin-aarch64": {
                      "signature": signature,
                      # The query string is a cache-buster, and it works because
                      # this pull zone keys its cache on the full URL including
                      # the query. Each release advertises a URL no edge has
                      # ever cached, so the updater download cannot be served a
                      # stale or spliced object even if every purge fails. Brew
                      # still fetches the bare URL and relies on the purge.
                      "url": f"{base}/AgencyZero.app.tar.gz?v={version}",
                  }
              },
          }
          with open("latest.json", "w") as f:
              json.dump(manifest, f, indent=2)
              f.write("\n")
          PY
          cat latest.json

      # Ordering matters and is the whole reason these are separate steps. The
      # tarball goes live and is confirmed live *before* latest.json advertises
      # it. Reversed, a client reads the new version, fetches an edge-cached
      # older tarball, and fails the signature check.
      #
      # The DELETE before the PUT is not tidiness: replacing the object instead
      # of overwriting it in place is what stops an edge from pulling a file
      # mid-write and caching the truncation (see the header comment).
      - name: Upload tarball to BunnyCDN
        env:
          STORAGE_API_KEY: ${{ secrets.BUNNYCDN_STORAGE_API_KEY }}
          STORAGE_NAME: ${{ secrets.BUNNYCDN_STORAGE_NAME }}
        run: |
          # A truncated or corrupt local build must never reach storage; after
          # this gate, any corruption seen at the edge is the CDN's doing.
          gunzip -t "$BUNDLE_DIR/AgencyZero.app.tar.gz" || {
            echo "::error::the built tarball is not a valid gzip"; exit 1;
          }
          upload() {
            local file="$1" dest="$2" attempt=1 code
            curl -sS -X DELETE -H "AccessKey: $STORAGE_API_KEY" \
              "https://storage.bunnycdn.com/$STORAGE_NAME/$dest" || true
            while [ "$attempt" -le 4 ]; do
              code=$(curl -sS -X PUT -H "AccessKey: $STORAGE_API_KEY" \
                -T "$file" -o /dev/null -w '%{http_code}' \
                "https://storage.bunnycdn.com/$STORAGE_NAME/$dest")
              case "$code" in
                200|201) echo "uploaded $dest"; return 0 ;;
              esac
              echo "::warning::attempt $attempt for $dest: HTTP $code"
              attempt=$((attempt + 1))
              sleep $((attempt * 3))
            done
            echo "::error::upload failed for $dest"
            return 1
          }
          upload "$BUNDLE_DIR/AgencyZero.app.tar.gz" "$CDN_DIR/AgencyZero.app.tar.gz"

      # Zone-wide, never per-URL: the per-URL endpoint reports success without
      # evicting (observed while the poisoned 0.1.1 object shrugged off three
      # purges), and the site sharing this zone purges the whole zone on every
      # deploy of its own, so there is no cache worth sparing.
      - name: Purge the pull zone
        env:
          ZONE_API_KEY: ${{ secrets.BUNNYCDN_ZONE_API_KEY }}
          ZONE_ID: ${{ secrets.BUNNYCDN_ZONE_ID }}
        run: |
          if [ -z "$ZONE_API_KEY" ] || [ -z "$ZONE_ID" ]; then
            echo "::error::BUNNYCDN_ZONE_API_KEY and BUNNYCDN_ZONE_ID are required: a versionless URL that is not purged serves the previous release forever."
            exit 1
          fi
          curl -sS --fail-with-body -X POST -H "AccessKey: $ZONE_API_KEY" \
            "https://bunnycdn.com/api/pullzone/$ZONE_ID/purgeCache"
          echo "purged pull zone $ZONE_ID"

      - name: Confirm the live tarball is the one just built
        env:
          ZONE_API_KEY: ${{ secrets.BUNNYCDN_ZONE_API_KEY }}
          ZONE_ID: ${{ secrets.BUNNYCDN_ZONE_ID }}
        run: |
          LOCAL=$(shasum -a 256 "$BUNDLE_DIR/AgencyZero.app.tar.gz" | cut -d' ' -f1)
          for attempt in 1 2 3 4 5 6; do
            curl -sS -L -o edge.tar.gz "$CDN_BASE/AgencyZero.app.tar.gz" || true
            REMOTE=$(shasum -a 256 edge.tar.gz | cut -d' ' -f1)
            # The sha proves identity; gunzip proves the edge did not cache a
            # partial response. Both, so the failure message names the culprit.
            if [ "$LOCAL" = "$REMOTE" ] && gunzip -t edge.tar.gz; then
              echo "edge matches build ($LOCAL) and the gzip is intact"
              exit 0
            fi
            echo "attempt $attempt: edge has $REMOTE, want $LOCAL"
            if [ "$attempt" = 3 ]; then
              echo "::warning::edge still stale after purge; purging the zone again"
              curl -sS --fail-with-body -X POST -H "AccessKey: $ZONE_API_KEY" \
                "https://bunnycdn.com/api/pullzone/$ZONE_ID/purgeCache" || true
            fi
            sleep 10
          done
          echo "::error::edge never served the new tarball; not publishing latest.json"
          exit 1

      - name: Publish latest.json
        env:
          VERSION: ${{ needs.check.outputs.version }}
          STORAGE_API_KEY: ${{ secrets.BUNNYCDN_STORAGE_API_KEY }}
          STORAGE_NAME: ${{ secrets.BUNNYCDN_STORAGE_NAME }}
          ZONE_API_KEY: ${{ secrets.BUNNYCDN_ZONE_API_KEY }}
          ZONE_ID: ${{ secrets.BUNNYCDN_ZONE_ID }}
        run: |
          curl -sS -X DELETE -H "AccessKey: $STORAGE_API_KEY" \
            "https://storage.bunnycdn.com/$STORAGE_NAME/$CDN_DIR/latest.json" || true
          curl -sS --fail-with-body -X PUT -H "AccessKey: $STORAGE_API_KEY" \
            -T latest.json "https://storage.bunnycdn.com/$STORAGE_NAME/$CDN_DIR/latest.json"
          curl -sS --fail-with-body -X POST -H "AccessKey: $ZONE_API_KEY" \
            "https://bunnycdn.com/api/pullzone/$ZONE_ID/purgeCache"

          # latest.json is the version record, so a stale edge copy here means
          # nobody sees the release. It is also what the next run compares
          # against, so a stale copy would republish this version forever.
          for attempt in 1 2 3 4 5 6; do
            LIVE=$(curl -sS -L "$CDN_BASE/latest.json" | python3 -c 'import json,sys; print(json.load(sys.stdin)["version"])' 2>/dev/null || echo "")
            if [ "$LIVE" = "$VERSION" ]; then
              echo "edge serves version $LIVE"
              exit 0
            fi
            echo "attempt $attempt: edge serves '$LIVE', want '$VERSION'"
            if [ "$attempt" = 3 ]; then
              echo "::warning::edge still stale after purge; purging the zone again"
              curl -sS --fail-with-body -X POST -H "AccessKey: $ZONE_API_KEY" \
                "https://bunnycdn.com/api/pullzone/$ZONE_ID/purgeCache" || true
            fi
            sleep 10
          done
          echo "::error::edge never served the new latest.json"
          exit 1

      - name: Summary
        env:
          VERSION: ${{ needs.check.outputs.version }}
        run: |
          {
            echo "### AgencyZero $VERSION published"
            echo
            echo "- Signing: **ad-hoc**, not notarized. Install via the Homebrew tap; a browser download will refuse to launch."
            echo "- Bundle: \`$CDN_BASE/AgencyZero.app.tar.gz\`"
            echo "- Manifest: \`$CDN_BASE/latest.json\`"
          } >> "$GITHUB_STEP_SUMMARY"
