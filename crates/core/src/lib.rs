//! Shared types and protocol definitions for agencyzero.
//!
//! Anything two or more executables need (message types, config schema,
//! wire formats) lives here so the binaries stay decoupled from each other.

/// Placeholder so the crate compiles; replace with real shared types.
pub const VERSION: &str = env!("CARGO_PKG_VERSION");
